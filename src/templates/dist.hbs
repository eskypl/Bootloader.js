(function (root, factory) {
	'use strict';
	if (typeof define === 'function' && define.amd) {
		// AMD. Register as an anonymous module.
		define([], factory);
	} else {
		root.{{{globalAlias}}} = factory(root);
	}
}(this, function (window) {
	'use strict';

	{{{code}}}
}));